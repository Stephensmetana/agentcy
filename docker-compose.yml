services:
  agentcy:
    build: .
    ports:
      - "9001:9001"
    volumes:
      # Persist the database and exports between container restarts
      - ./databases:/app/databases
      - ./chat_logs:/app/chat_logs
    restart: unless-stopped
